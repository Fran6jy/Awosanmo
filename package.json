{
  "name": "awosanmo",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted private cloud torrenting and streaming platform.",
  "author": "fran6",
  "contributors": [
    "fran6"
  ],
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @awosanmo/api & npm run dev --workspace @awosanmo/web",
    "build": "npm run build --workspace @awosanmo/api && npm run build --workspace @awosanmo/web",
    "test": "npm run test --workspace @awosanmo/api",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  }
}
